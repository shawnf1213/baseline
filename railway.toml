[build]
builder = "NIXPACKS"

[deploy]
# Dispatcher — one repo, two Railway services share this config.
# start.sh runs the Discord bot when SERVICE_ROLE=bot, otherwise the backend.
startCommand = "bash start.sh"
